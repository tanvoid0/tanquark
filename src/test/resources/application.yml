quarkus:
  datasource:
    db-kind: postgresql
    username: postgres
    password: postgres
    jdbc:
      url: jdbc:postgresql://localhost:5432/tanvoid0?currentSchema=tanquark_test
  hibernate-orm:
    dialect: org.hibernate.dialect.PostgreSQLDialect
    database:
      default-schema: tanquark_test
      generation: drop-and-create